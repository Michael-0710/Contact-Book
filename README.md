# Contact Book

A simple Python console app for adding, searching, deleting, and listing contacts. Contacts are saved in `contacts.json` beside the script so they remain available between runs.

## Run

```bash
python3 contact_book.py
```

Each contact has a required name and optional phone number and email address. Search matches any of those fields, and deleting a contact asks for confirmation.